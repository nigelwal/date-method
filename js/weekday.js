// Create an array and assign it values.
var weekday;
weekday = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];

// Show the first item from the array.
var el = document.getElementById("weekday");
el.textContent = weekday[2];